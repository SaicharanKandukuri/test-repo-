name: Get tree
on: [push]

jobs:
  GetTree:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: sudo apt update; sudo apt install tree -y
      - run: tree -L 4 /home
